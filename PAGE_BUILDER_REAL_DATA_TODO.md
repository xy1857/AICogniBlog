# 页面装修真实数据展示方案

## 问题分析

### 当前状态
- ❌ 所有组件使用**模拟数据**（mock data）
- ❌ 文章列表显示假数据
- ❌ 分类导航显示硬编码分类
- ❌ 友情链接显示示例链接
- ❌ 留言板/评论显示假评论
- ❌ 统计数据显示假数字

### 目标状态
- ✅ 文章网格显示**真实文章**
- ✅ 分类导航显示**真实分类**及文章数
- ✅ 友情链接显示**用户配置的真实链接**
- ✅ 留言板显示**真实留言**
- ✅ 评论墙显示**真实评论**
- ✅ 统计面板显示**真实统计数据**

---

## 方案设计

### 方案一：组件内部直接调用 API（推荐用于快速实现）

#### 优点
- 实现简单快速
- 每个组件独立管理数据
- 易于理解和维护

#### 缺点
- 可能有重复请求
- 组件耦合度较高
- 难以统一管理加载状态

#### 实现思路

```
组件挂载时
  ↓
调用对应 API
  ↓
获取真实数据
  ↓
渲染展示
```

**需要修改的组件：**

1. **ArticleGrid.vue**
   - 调用 `getArticles()` API
   - 支持分页、筛选、排序
   - 显示真实文章列表

2. **CategoryNav.vue**
   - 调用 `getCategories()` API
   - 获取分类列表和文章数
   - 支持点击筛选

3. **FriendLinks.vue**
   - 从用户配置中读取
   - 或调用 `getFriendLinks()` API
   - 显示真实友链

4. **InteractiveWidget.vue**
   - 留言板：调用 `getGuestbook()` API
   - 评论墙：调用 `getLatestComments()` API
   - 访客地图：调用 `getVisitorStats()` API

5. **StatsPanel.vue**（如果有）
   - 调用 `getStats()` API
   - 显示真实统计数据

---

### 方案二：通过 Store 统一管理（推荐用于生产环境）

#### 优点
- 数据统一管理
- 避免重复请求
- 支持缓存策略
- 易于实现加载状态

#### 缺点
- 实现相对复杂
- 需要设计 Store 结构

#### 实现思路

```
PagePreview.vue 加载时
  ↓
调用 Store actions 预加载所有数据
  ↓
Store 缓存数据
  ↓
组件从 Store 读取数据
  ↓
渲染展示
```

**需要创建的 Store：**

1. **useContentStore**
   - articles: 文章列表
   - categories: 分类列表
   - tags: 标签列表
   - comments: 评论列表
   - stats: 统计数据

2. **useFriendLinksStore**
   - links: 友情链接列表
   - 支持 CRUD 操作

3. **useGuestbookStore**
   - messages: 留言列表
   - 支持添加留言

---

### 方案三：混合方案（最佳实践）

#### 核心思想
- **静态数据**：通过 Store 统一管理（文章、分类、统计）
- **动态数据**：组件内部实时获取（留言、评论）
- **配置数据**：从页面配置读取（友情链接）

#### 数据分类

**1. 静态内容数据（Store 管理）**
- 文章列表
- 分类列表
- 标签云
- 统计数据

**2. 动态交互数据（组件管理）**
- 留言板
- 最新评论
- 访客地图

**3. 用户配置数据（配置管理）**
- 友情链接
- 自定义文本
- 社交链接

---

## 详细实现 TODO

### Phase 1: 基础数据接入（1-2天）

#### TODO 1.1: 文章网格真实数据
- [ ] 在 `ArticleGrid.vue` 中导入 `articleApi`
- [ ] 添加 `articles` 响应式变量
- [ ] 在 `onMounted` 中调用 `articleApi.getArticles()`
- [ ] 替换 `mockArticles` 为真实数据
- [ ] 处理加载状态（loading）
- [ ] 处理错误状态（error）
- [ ] 支持分页参数传递
- [ ] 支持按分类筛选

**数据结构映射：**
```typescript
// 后端返回
{
  id: number,
  title: string,
  content: string,
  createdAt: string,
  viewCount: number,
  tags: string[]
}

// 组件需要
{
  id: number,
  title: string,
  excerpt: string,  // 从 content 截取
  cover: string,    // 默认图或从配置读取
  tags: string[],
  date: string,     // 从 createdAt 转换
  readCount: number // 从 viewCount 映射
}
```

#### TODO 1.2: 分类导航真实数据
- [ ] 创建 `categoryApi.ts`（如果不存在）
- [ ] 添加 `getCategories()` API
- [ ] 在 `CategoryNav.vue` 中调用 API
- [ ] 获取分类列表和文章数
- [ ] 支持点击分类触发筛选事件
- [ ] 与 ArticleGrid 联动

**API 设计：**
```typescript
// GET /api/categories
{
  categories: [
    { id: string, name: string, count: number, icon?: string, color?: string }
  ]
}
```

#### TODO 1.3: 统计数据真实化
- [ ] 创建 `statsApi.ts`
- [ ] 添加 `getStats()` API
- [ ] 在 `InteractiveWidget.vue` 中调用
- [ ] 显示真实访问量、文章数等

**API 设计：**
```typescript
// GET /api/stats
{
  totalViews: number,
  todayViews: number,
  articleCount: number,
  commentCount: number,
  visitorCountries: number
}
```

---

### Phase 2: 交互数据接入（2-3天）

#### TODO 2.1: 留言板真实数据
- [ ] 创建 `guestbookApi.ts`
- [ ] 添加 `getMessages()` API
- [ ] 添加 `postMessage()` API
- [ ] 在 `InteractiveWidget.vue` 中集成
- [ ] 实现真实留言提交
- [ ] 实现留言列表加载
- [ ] 添加分页或无限滚动

**API 设计：**
```typescript
// GET /api/guestbook
{
  messages: [
    {
      id: number,
      name: string,
      content: string,
      avatar: string,
      createdAt: string
    }
  ]
}

// POST /api/guestbook
{
  name: string,
  content: string
}
```

#### TODO 2.2: 评论墙真实数据
- [ ] 使用现有 `commentApi`
- [ ] 调用 `getLatestComments()` API
- [ ] 在 `InteractiveWidget.vue` 中显示
- [ ] 显示最新评论列表
- [ ] 支持跳转到文章

**数据映射：**
```typescript
// 后端返回
{
  id: number,
  content: string,
  articleId: number,
  articleTitle: string,
  userId: number,
  username: string,
  createdAt: string
}

// 组件需要
{
  id: number,
  author: string,      // 从 username 映射
  article: string,     // 从 articleTitle 映射
  content: string,
  time: string,        // 从 createdAt 转换
  avatar: string       // 生成或从用户信息获取
}
```

---

### Phase 3: 配置数据管理（1-2天）

#### TODO 3.1: 友情链接配置化
- [ ] 在页面配置中添加 `friendLinks` 字段
- [ ] 在装修器中添加友链管理界面
- [ ] 支持添加/编辑/删除友链
- [ ] 在 `FriendLinks.vue` 中读取配置
- [ ] 如果配置为空，显示默认链接

**配置结构：**
```typescript
interface PageConfig {
  // ...
  friendLinks: Array<{
    id: string,
    name: string,
    url: string,
    logo?: string,
    description?: string
  }>
}
```

#### TODO 3.2: 社交链接配置
- [ ] 在 Hero 组件中添加社交链接
- [ ] 支持配置 GitHub、Twitter、Email 等
- [ ] 从页面配置读取

---

### Phase 4: 优化和增强（2-3天）

#### TODO 4.1: 加载状态优化
- [ ] 添加骨架屏（Skeleton）
- [ ] 添加加载动画
- [ ] 优化首屏加载速度
- [ ] 实现懒加载

#### TODO 4.2: 错误处理
- [ ] API 调用失败时显示友好提示
- [ ] 数据为空时显示空状态
- [ ] 网络错误时显示重试按钮

#### TODO 4.3: 缓存策略
- [ ] 实现数据缓存（5分钟）
- [ ] 避免重复请求
- [ ] 支持手动刷新

#### TODO 4.4: 实时更新
- [ ] 留言板实时更新（可选）
- [ ] 评论数实时更新（可选）
- [ ] 使用 WebSocket 或轮询

---

## 数据流设计

### 方案一：组件直接调用（简单）

```
PagePreview.vue
  ↓
渲染各个组件
  ↓
ArticleGrid.vue → articleApi.getArticles() → 显示文章
CategoryNav.vue → categoryApi.getCategories() → 显示分类
FriendLinks.vue → 读取 pageConfig.friendLinks → 显示友链
InteractiveWidget.vue → guestbookApi.getMessages() → 显示留言
```

### 方案二：Store 统一管理（推荐）

```
PagePreview.vue
  ↓
onMounted() {
  await contentStore.loadArticles()
  await contentStore.loadCategories()
  await contentStore.loadStats()
}
  ↓
各组件从 Store 读取数据
  ↓
ArticleGrid.vue → contentStore.articles
CategoryNav.vue → contentStore.categories
InteractiveWidget.vue → contentStore.stats
```

---

## API 清单

### 需要的后端 API

#### 1. 文章相关
```
GET /api/articles?page=1&size=10&category=tech
GET /api/articles/:id
```

#### 2. 分类相关
```
GET /api/categories
GET /api/categories/:id/articles
```

#### 3. 统计相关
```
GET /api/stats
GET /api/stats/visitor
```

#### 4. 留言板相关
```
GET /api/guestbook?page=1&size=10
POST /api/guestbook
DELETE /api/guestbook/:id
```

#### 5. 评论相关（已存在）
```
GET /api/comments/latest?limit=10
```

#### 6. 友情链接相关（可选）
```
GET /api/friend-links
POST /api/friend-links
PUT /api/friend-links/:id
DELETE /api/friend-links/:id
```

---

## 实现优先级

### P0 - 必须实现（核心功能）
1. ✅ 文章网格显示真实文章
2. ✅ 分类导航显示真实分类
3. ✅ 基础加载状态

### P1 - 应该实现（重要功能）
4. ✅ 留言板真实数据
5. ✅ 评论墙真实数据
6. ✅ 统计数据真实化
7. ✅ 错误处理

### P2 - 可以实现（增强功能）
8. ⭐ 友情链接配置化
9. ⭐ 数据缓存
10. ⭐ 骨架屏

### P3 - 未来实现（高级功能）
11. 🚀 实时更新
12. 🚀 数据预加载
13. 🚀 离线支持

---

## 时间估算

| 阶段 | 任务 | 预计时间 |
|------|------|---------|
| Phase 1 | 基础数据接入 | 1-2天 |
| Phase 2 | 交互数据接入 | 2-3天 |
| Phase 3 | 配置数据管理 | 1-2天 |
| Phase 4 | 优化和增强 | 2-3天 |
| **总计** | | **6-10天** |

---

## 风险和注意事项

### 风险
1. **后端 API 可能不存在**
   - 解决：先实现前端，使用 mock 数据
   - 后端 API 就绪后再对接

2. **数据结构不匹配**
   - 解决：添加数据转换层（adapter）
   - 统一数据格式

3. **性能问题**
   - 解决：实现分页、懒加载
   - 添加缓存机制

### 注意事项
1. **向后兼容**
   - 如果 API 失败，降级到 mock 数据
   - 不影响现有功能

2. **用户体验**
   - 添加加载动画
   - 优化首屏加载
   - 处理空状态

3. **安全性**
   - 留言板需要防 XSS
   - 评论需要审核机制
   - API 需要鉴权

---

## 推荐实施路径

### 第一步：快速验证（1天）
- 只实现 ArticleGrid 真实数据
- 验证数据流是否正确
- 确认 API 是否可用

### 第二步：核心功能（3-4天）
- 实现所有 P0 和 P1 功能
- 确保基本可用

### 第三步：优化增强（2-3天）
- 实现 P2 功能
- 优化用户体验

### 第四步：持续迭代
- 根据用户反馈优化
- 实现 P3 功能

---

## 总结

**推荐方案**：混合方案（方案三）
- 静态数据用 Store 管理
- 动态数据组件内获取
- 配置数据从 pageConfig 读取

**实施顺序**：
1. 文章网格（最重要）
2. 分类导航（联动）
3. 统计数据（简单）
4. 留言板/评论（交互）
5. 友情链接（配置）
6. 优化增强（体验）

**预计时间**：6-10 个工作日

**关键成功因素**：
- 后端 API 支持
- 数据结构设计
- 错误处理完善
- 用户体验优化

